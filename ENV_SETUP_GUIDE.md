# 🔧 Руководство по настройке .env

Пошаговая инструкция по настройке переменных окружения для my-webs.ru

---

## 📋 Что есть в репозитории

1. **`.env.example`** - шаблон для разработки
2. **`.env.production`** - готовый шаблон для продакшена ⭐

---

## 🚀 Быстрая настройка (5 минут)

### На сервере выполните:

```bash
cd /var/www/digagru

# Скопировать production шаблон
cp .env.production .env

# Открыть для редактирования
nano .env
```

---

## 🔐 Обязательные настройки

### 1. SESSION_SECRET (КРИТИЧНО!)

**Что это:** Ключ шифрования сессий пользователей

**Уже сгенерировано в .env.production:**
```env
SESSION_SECRET=mfahAWTSaWQqL8zieWgZSWhj956fGn1XkpKs66Mzzn8=
```

✅ **Можно оставить как есть** (уже безопасный ключ)

Или сгенерировать новый:
```bash
openssl rand -base64 32
# Вывод: ScJf9K2h... (используйте этот ключ)
```

**❌ НЕ ИСПОЛЬЗУЙТЕ:**
- Простые слова
- Короткие строки (< 32 символов)
- Ключи из примеров

---

### 2. ADMIN_PASSWORD (КРИТИЧНО!)

**Что это:** Пароль для входа в админ-панель

**Текущее значение:**
```env
ADMIN_PASSWORD=ChangeMeToStrongPassword123!
```

❌ **ОБЯЗАТЕЛЬНО ИЗМЕНИТЬ!**

**Рекомендации:**
- Минимум 12 символов
- Буквы, цифры, спецсимволы
- Не используйте личную информацию

**Примеры сильных паролей:**
```bash
# Сгенерировать случайный пароль
openssl rand -base64 16
# Вывод: 7tK9mPq2nL4xR8wZ5yV3

# Или придумайте свой
Admin_MyWebs_2025_SecurePass!
```

---

### 3. ADMIN_USERNAME (опционально)

**Что это:** Логин для админ-панели

**По умолчанию:**
```env
ADMIN_USERNAME=admin
```

✅ **Можно оставить** или изменить на свой:
```env
ADMIN_USERNAME=myusername
```

---

## 📧 Email уведомления (опционально)

### Настройка Gmail

**1. Включите 2FA в Google аккаунте:**
https://myaccount.google.com/security

**2. Создайте пароль приложения:**
https://myaccount.google.com/apppasswords

Выберите:
- Приложение: "Почта"
- Устройство: "Другое" → введите "DigAgRu"

**3. Скопируйте 16-значный пароль**

**4. Обновите .env:**
```env
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=abcd efgh ijkl mnop  # 16-значный пароль приложения
EMAIL_TO=notifications@my-webs.ru
```

### Другие почтовые сервисы

#### Yandex Mail
```env
EMAIL_SERVICE=smtp
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=your-email@yandex.ru
EMAIL_PASS=your-password
EMAIL_TO=notifications@my-webs.ru
```

#### Mail.ru
```env
EMAIL_SERVICE=smtp
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=your-email@mail.ru
EMAIL_PASS=your-password
EMAIL_TO=notifications@my-webs.ru
```

#### Custom SMTP
```env
EMAIL_SERVICE=smtp
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=username
EMAIL_PASS=password
EMAIL_TO=notifications@my-webs.ru
```

---

## 📱 Telegram уведомления (опционально)

### Создание бота

**1. Откройте Telegram, найдите @BotFather**

**2. Отправьте команду:**
```
/newbot
```

**3. Следуйте инструкциям:**
- Введите имя бота: "DigAgRu Notifications"
- Введите username: "digagru_bot" (должен заканчиваться на _bot)

**4. Скопируйте токен:**
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### Получение CHAT_ID

**Способ 1 - Через бота @userinfobot:**
1. Найдите @userinfobot в Telegram
2. Нажмите Start
3. Бот отправит вам ваш ID

**Способ 2 - Через API:**
1. Напишите своему боту любое сообщение
2. Откройте в браузере:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
```
Замените `<YOUR_BOT_TOKEN>` на токен вашего бота

3. Найдите в JSON:
```json
"chat": {
  "id": 123456789,
  ...
}
```

### Обновите .env:
```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

---

## 🎯 Финальный .env для production

Минимальная рабочая конфигурация:

```env
NODE_ENV=production
PORT=3000
SESSION_SECRET=mfahAWTSaWQqL8zieWgZSWhj956fGn1XkpKs66Mzzn8=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=YourStrongPassword123!
SITE_URL=https://my-webs.ru
AGENCY_NAME=DigAgRu
```

С уведомлениями (полная):

```env
NODE_ENV=production
PORT=3000
SESSION_SECRET=mfahAWTSaWQqL8zieWgZSWhj956fGn1XkpKs66Mzzn8=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=YourStrongPassword123!
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_TO=notifications@my-webs.ru
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
SITE_URL=https://my-webs.ru
AGENCY_NAME=DigAgRu
```

---

## ✅ Проверка конфигурации

### На сервере:

```bash
cd /var/www/digagru

# Проверить что файл существует
ls -la .env

# Проверить права (должен быть доступен только владельцу)
chmod 600 .env

# Посмотреть содержимое (БЕЗ вывода в логи!)
cat .env
```

### Тест запуска:

```bash
# Временный запуск для проверки
npm start

# Если всё ОК, нажмите Ctrl+C и запустите через PM2
pm2 start server/index.js --name digagru
pm2 logs digagru
```

**Признаки правильной настройки:**
- ✅ "Connected to SQLite database"
- ✅ "Server is running on http://localhost:3000"
- ✅ "Default admin user created" (при первом запуске)

**Признаки проблем:**
- ❌ "SESSION_SECRET is required"
- ❌ "Error creating default admin"
- ❌ Приложение сразу падает

---

## 🔒 Безопасность

### ✅ Что нужно делать:

1. **Изменить SESSION_SECRET** (или оставить автогенерированный)
2. **Изменить ADMIN_PASSWORD** на сильный пароль
3. **Установить права на файл:**
   ```bash
   chmod 600 /var/www/digagru/.env
   ```
4. **НЕ коммитить .env в Git** (уже в .gitignore)
5. **Делать бэкап .env:**
   ```bash
   cp .env /root/backups/env_backup_$(date +%Y%m%d).txt
   ```

### ❌ Чего НЕ делать:

- Не используйте дефолтные пароли
- Не публикуйте .env в интернете
- Не отправляйте .env по email без шифрования
- Не храните пароли в открытом виде

---

## 🔄 Обновление .env

### После изменений:

```bash
# Перезапустить приложение
pm2 restart digagru

# Проверить логи
pm2 logs digagru --lines 50
```

---

## 📝 Шпаргалка

| Переменная | Обязательна | Что делает |
|------------|-------------|------------|
| `SESSION_SECRET` | ✅ Да | Шифрование сессий |
| `ADMIN_PASSWORD` | ✅ Да | Пароль админки |
| `ADMIN_USERNAME` | ⚠️ Нет | Логин админки (default: admin) |
| `EMAIL_*` | ⚠️ Нет | Email уведомления |
| `TELEGRAM_*` | ⚠️ Нет | Telegram уведомления |
| `SITE_URL` | ✅ Да | URL сайта для ссылок |

---

## 🆘 Проблемы и решения

### "Cannot find module 'dotenv'"
```bash
cd /var/www/digagru
npm install
```

### "SESSION_SECRET is required"
Проверьте что в .env есть строка:
```env
SESSION_SECRET=ваш_длинный_ключ_минимум_32_символа
```

### Email не отправляются
1. Проверьте пароль приложения (для Gmail)
2. Проверьте логи: `pm2 logs digagru`
3. Временно отключите: закомментируйте EMAIL_* переменные

### Telegram не работает
1. Проверьте токен бота
2. Проверьте CHAT_ID (должно быть число)
3. Напишите боту /start
4. Проверьте логи: `pm2 logs digagru`

---

## 📞 Готовые команды

### Создать .env на сервере (копировать всё):

```bash
cat > /var/www/digagru/.env << 'EOF'
NODE_ENV=production
PORT=3000
SESSION_SECRET=mfahAWTSaWQqL8zieWgZSWhj956fGn1XkpKs66Mzzn8=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeMe123!
SITE_URL=https://my-webs.ru
AGENCY_NAME=DigAgRu
