# DigAgRu - Digital Agency Website

Одностраничный многоязычный сайт для микро-агентства, занимающегося созданием, продвижением и поддержкой сайтов для малого бизнеса.

## Возможности

### Frontend
- 🌐 Многоязычность (Русский/English)
- 🌓 Светлая и тёмная темы
- 📱 Адаптивный дизайн
- ⚡ Современный UI с Tailwind CSS
- 🎨 Анимации и плавные переходы

### Backend
- 🔐 Админ-панель с аутентификацией
- 📊 CRM для управления заявками
- 📈 Встроенная аналитика и статистика
- ✉️ Email уведомления (Nodemailer)
- 📱 Telegram уведомления
- 💾 SQLite база данных

### Функции
- Управление заявками (новые, в работе, завершённые)
- Управление отзывами клиентов
- Контактная форма с валидацией
- Статистика по периодам (сегодня, неделя, месяц)

## Установка

### 1. Клонирование репозитория
\`\`\`bash
git clone <repository-url>
cd digAgRu
\`\`\`

### 2. Установка зависимостей
\`\`\`bash
npm install
\`\`\`

### 3. Настройка окружения
Создайте файл `.env` на основе `.env.example`:
\`\`\`bash
cp .env.example .env
\`\`\`

Отредактируйте `.env` и настройте переменные:
\`\`\`env
# Server Configuration
PORT=3000
SESSION_SECRET=your-secret-key-change-this

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Email Configuration (optional)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_TO=notifications@youragency.com

# Telegram Bot Configuration (optional)
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id

# Site Configuration
SITE_URL=http://localhost:3000
AGENCY_NAME=DigAgRu
\`\`\`

### 4. Настройка Tailwind CSS (опционально)
Если хотите кастомизировать стили:
\`\`\`bash
npm run build:css
\`\`\`

## Запуск

### Development режим
\`\`\`bash
npm run dev
\`\`\`

### Production режим
\`\`\`bash
npm start
\`\`\`

Сайт будет доступен по адресу: `http://localhost:3000`

Админ-панель: `http://localhost:3000/admin`

**Данные для входа по умолчанию:**
- Username: `admin`
- Password: `admin123`

## Структура проекта

\`\`\`
digAgRu/
├── server/
│   ├── index.js           # Основной сервер Express
│   ├── database.js        # Настройка базы данных SQLite
│   └── notifications.js   # Email и Telegram уведомления
├── public/
│   ├── css/
│   │   ├── input.css      # Исходные стили Tailwind
│   │   └── output.css     # Скомпилированные стили
│   ├── js/
│   │   ├── app.js         # Frontend логика (язык, тема, формы)
│   │   └── admin.js       # Админ-панель логика
│   └── index.html         # Главная страница
├── views/
│   ├── admin-login.ejs    # Страница входа
│   └── admin-dashboard.ejs # Админ-панель
├── data/
│   └── agency.db          # База данных SQLite (создаётся автоматически)
├── .env.example           # Пример конфигурации
├── package.json
├── tailwind.config.js
├── README.md
└── CLAUDE.md
\`\`\`

## Настройка уведомлений

### Email (Gmail)
1. Включите 2FA в вашем Gmail аккаунте
2. Создайте App Password: https://myaccount.google.com/apppasswords
3. Используйте этот пароль в `.env`:
   \`\`\`env
   EMAIL_USER=your-email@gmail.com
   EMAIL_PASS=your-app-password
   \`\`\`

### Telegram
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Получите ваш Chat ID через [@userinfobot](https://t.me/userinfobot)
4. Добавьте в `.env`:
   \`\`\`env
   TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
   TELEGRAM_CHAT_ID=123456789
   \`\`\`

## API Endpoints

### Публичные
- `GET /` - Главная страница
- `GET /api/testimonials` - Получить отзывы
- `POST /api/contact` - Отправить заявку

### Админ (требуется авторизация)
- `GET /admin` - Админ-панель
- `POST /admin/login` - Вход
- `GET /admin/logout` - Выход
- `GET /api/admin/leads` - Получить все заявки
- `PUT /api/admin/leads/:id` - Обновить статус заявки
- `DELETE /api/admin/leads/:id` - Удалить заявку
- `GET /api/admin/stats` - Получить статистику
- `GET /api/admin/testimonials` - Получить все отзывы (включая скрытые)
- `POST /api/admin/testimonials` - Создать отзыв
- `PUT /api/admin/testimonials/:id` - Обновить отзыв
- `DELETE /api/admin/testimonials/:id` - Удалить отзыв

## Кастомизация

### Изменение цветов
Отредактируйте `tailwind.config.js`:
\`\`\`javascript
colors: {
    primary: {
        // Ваша цветовая палитра
    }
}
\`\`\`

### Добавление переводов
Отредактируйте объект `translations` в `public/js/app.js`

### Изменение контента
Основной контент находится в `public/index.html`

## Deployment

### Heroku
\`\`\`bash
heroku create your-app-name
heroku config:set NODE_ENV=production
git push heroku main
\`\`\`

### VPS/DigitalOcean
1. Установите Node.js
2. Клонируйте репозиторий
3. Установите PM2: `npm install -g pm2`
4. Запустите: `pm2 start server/index.js --name digagru`
5. Настройте Nginx как reverse proxy

## Безопасность

⚠️ **Важно:**
- Измените `SESSION_SECRET` на случайную строку
- Измените пароль администратора после первого входа
- Используйте HTTPS в production
- Регулярно обновляйте зависимости

## Поддержка

Для вопросов и предложений создайте Issue в репозитории.

## Лицензия

MIT
